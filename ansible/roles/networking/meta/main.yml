---
galaxy_info:
  role_name: networking
  description: Netplan VLAN sub-interface configuration
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [jammy]
dependencies:
  - role: common
