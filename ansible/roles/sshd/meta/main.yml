---
galaxy_info:
  role_name: sshd
  description: OpenSSH hardening, bound to external interface only
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [jammy]
dependencies:
  - role: common
  - role: networking
